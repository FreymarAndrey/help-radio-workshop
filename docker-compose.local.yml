services:
  react-client-up:
    container_name: react-client-up
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - 3030:3030
    volumes:
      - ./src/src:/app/src
      - /app/node_modules
